# 样式规范（2026-04 现状校准版）

## 样式方案概述

项目采用 **CSS Modules + CSS Variables 设计系统**，当前前端视觉语言为：

- Apple 风格玻璃态（Glassmorphism）
- 渐变/径向背景氛围（主场景层）
- 轻量过渡与微动效（0.15s~0.4s）
- Light-first + Dark override（`prefers-color-scheme`）
- 桌面与触控差异化交互（`pointer: coarse`）
- 本地媒体图库采用克制的沉浸式瀑布流：照片/视频内容是视觉主角，控件保持玻璃态、轻量、可扫描
- 模型资产库采用与本地媒体图库同源的玻璃态工具栏、分段筛选和沉浸式网格：模型封面是视觉主角，详情与操作保持可达但不压过内容

**不使用**：CSS-in-JS、Tailwind、Sass/Less。

> 🎨 进行 UI 设计或界面改进时，可调用 Skill：[.agents/skills/ui-ux-pro-max/SKILL.md](../skills/ui-ux-pro-max/SKILL.md) 获取设计系统推荐（配色、字体、布局模式等）。

---

## 样式架构与文件职责

当前样式入口链路：

1. `main.tsx` 引入 `styles/global.css`
2. `global.css` 再 `@import` `variables.css` + `animations.css`
3. `App.css` 负责页面级布局（主容器、启动屏、空态、全局遮罩）
4. 各组件使用 `*.module.css` 维护局部样式

| 文件 | 职责 | 规则 |
|------|------|------|
| `frontend/src/styles/variables.css` | 全局设计 Token（颜色、间距、圆角、过渡、玻璃、布局） | 新变量必须加在这里 |
| `frontend/src/styles/animations.css` | 可复用全局关键帧 | 跨组件复用动画优先放这里 |
| `frontend/src/styles/global.css` | Reset、基础标签样式、全局辅助类 | 保持轻量，避免业务样式堆积 |
| `frontend/src/App.css` | 页面级布局与场景样式 | 仅放全局页面结构与状态样式 |
| `frontend/src/components/**/**.module.css` | 组件局部样式 | 不污染全局命名空间 |

本地媒体图库、模型资产库和局域网门禁相关控件应优先复用 `components/common/SelectMenu`、`ConfirmDialog`、`TextInputDialog` 等通用玻璃态组件，避免浏览器原生 `select` / `prompt` / `confirm` / `alert` 造成视觉割裂。

视频重建相关 UI（生成弹窗、依赖状态、质量/引擎分段控件、任务错误提示、模型来源操作）必须和 Settings、侧栏、媒体预览保持同一套 Apple 玻璃态语言：半透明面板、柔和边框、可读文本层级、克制状态色和系统浅色/深色模式适配。不要为了快速上线引入白底表单、原生 alert 或高饱和错误条。

> `frontend/src/index.css` 中仍有历史遗留变量定义；新增变量不要放在 `index.css`。

---

## CSS Modules 与命名约定

每个组件样式文件使用 `.module.css`。

| 范围 | 命名规范 | 示例 |
|------|----------|------|
| CSS Modules class | camelCase | `.actionBtn`, `.sectionTitle`, `.statusIcon` |
| 全局 class（`App.css` / `global.css`） | kebab-case | `.boot-screen`, `.empty-state` |
| CSS 变量 | kebab-case + `--` 前缀 | `--accent-blue`, `--glass-bg` |

推荐 className 组合模式：

```typescript
const classes = [
  styles.button,
  styles[variant],
  disabled && styles.disabled,
  className,
].filter(Boolean).join(' ');
```

---

## 设计 Token 规范

### 必须 Token 化的维度

- 间距：使用 `--space-*`
- 圆角：使用 `--radius-*`
- 过渡：使用 `--transition-*`
- 常用文本与语义色：使用 `--text-*`、`--accent-*`、`--success|warning|danger|error`
- 布局关键尺寸：使用 `--sidebar-width`、`--controls-height`

### 可以局部硬编码的维度

以下值允许在组件中按视觉需要局部硬编码（常见于玻璃态层）：

- 半透明层 `rgba(...)`
- 特定阴影 `box-shadow`
- 局部渐变背景

但同一数值在 3 处以上重复时，应提炼为变量并迁移到 `variables.css`。

### 玻璃态标准写法

```css
.panel {
  background: var(--glass-bg);
  backdrop-filter: var(--glass-blur);
  -webkit-backdrop-filter: var(--glass-blur);
  border: 1px solid var(--glass-border);
  box-shadow: var(--glass-shadow);
}
```

玻璃态浮动/粘性控件需要拆分布局层与视觉层时，应把 `backdrop-filter`、玻璃背景、边框和阴影放在真实可见的玻璃表面；布局层只负责占位、sticky/fixed 定位和命中区域。为保证模糊能采样到背后的页面内容，避免在玻璃表面的父级无意创建新的渲染/采样上下文，例如滥用 `transform`、`will-change`、`isolation: isolate`、`filter` 或不必要的 `contain`。

需要为动画面板保留稳定占位时，测量值应代表布局尺寸，而不是动画后的视觉尺寸。若面板使用 `transform` 做缩放、吸附或进出场，避免直接用 `getBoundingClientRect().height` 驱动占位高度；优先使用 `offsetHeight` 或等价的布局尺寸，防止装饰性动效反向影响列表、网格或滚动内容布局。

---

## 字体与排版

当前项目使用 Apple 系统字体栈：

```css
font-family: -apple-system, BlinkMacSystemFont, "SF Pro Display", "SF Pro Text",
             "Helvetica Neue", Arial, sans-serif;
```

规范建议：

- 页面级字号优先 12/13/14/16/18 的层级节奏
- 文本颜色优先使用 `--text-primary` / `--text-secondary` / `--text-muted`
- 保证正文与背景对比度至少 4.5:1

---

## 动画与过渡

### 时长与缓动

- 交互反馈：0.15s~0.3s
- 面板/遮罩进出场：0.25s~0.4s
- 优先使用 `cubic-bezier(0.16, 1, 0.3, 1)` 或 Token 中既有过渡
- 吸附/回弹动效只应作用在一个清晰层级上，通常是整块面板本身；避免让面板和内部多个子控件同时使用过冲曲线，真实移动设备上容易显得抖动和生硬。
- 布局占位、列表流和网格高度不应跟随装饰性动效变化；动画可以改变内容层的 `transform`/`opacity`，但不应让周围内容被推挤。

### 动画放置规则

- 跨组件通用动画：放 `styles/animations.css`
- 强业务耦合动画：可放组件 `.module.css`
- 避免在多个组件重复声明同名 `@keyframes`（当前存在历史重复，后续逐步收敛）

### 可访问性

- 新增较明显动画时，需支持 `prefers-reduced-motion`
- 不要使用高频闪烁或大幅位移动画

---

## 响应式与输入设备策略

### 断点（按当前实现）

| 区间 | 规则 |
|------|------|
| `<= 768px` | 移动端：侧栏抽屉化、按钮与面板尺寸收紧 |
| `>= 769px` | 桌面端：侧栏浮动布局、主视图偏移补偿 |

### 输入方式适配

- 触控设备使用 `@media (pointer: coarse)` 做交互特化
- 仅 hover 可见的操作按钮在触控设备上应改为常驻或可达
- 全屏/主容器优先使用 `100dvh`，并保留 `100vh` fallback
- 本地媒体图库移动端默认不应只有单列；展示密度需要可调，且可保留双指捏合调整列数。
- 图库列数/密度浮层应以触发按钮为中心定位，并在窄屏下限制最大宽度，避免贴边或遮挡关键内容。
- 长列表、瀑布流或内部滚动容器需要提供可发现的滚动反馈：滚动条可以保持细、克制、半透明，但不能完全不可见；内容很长时应提供不遮挡主要操作的回到顶部入口。

### 本地媒体图库视觉细节

- 瀑布流卡片标题默认使用白色文字 + 阴影，hover/focus 时再出现半透明背景，不要让标题条常驻占据小缩略图的大面积空间。
- 排序标签需要同时保留字段名和方向提示，例如 `修改时间↓`、`名称 A-Z`；不要为了极简牺牲可读性。
- 图库密度控制使用图标触发、滑块调节和当前档位提示；避免多个 `+` / `-` 文本按钮堆在工具栏里。
- 主要动作如 `转换为 3D` 保持项目蓝色强调；次级图标使用当前文本色，不单独引入割裂的高饱和色。
- 全部/照片/视频类型筛选应使用共享背景的分段滑块形态，文字和数字保持同一文本层级；不要给数字单独加突兀椭圆徽标。
- 工具栏按钮、分段筛选、排序、密度、上传/选择等控件高度应保持一致；中间宽度下允许换行重排，但不能截断按钮文字或让文字竖排。
- 移动端工具栏或密集控制面板中的弱提示文案仍需可读：字号、行高、字重和对比度要能在真实手机上辨认；可以使用次级文本色与透明度形成弱层级，不要用过淡灰色硬压低信息。
- 移动端工具栏的信息区与主要控制区之间应保持紧凑关系，避免玻璃面板内部出现无意义大空白；控件组之间的留白应服务于扫描和触控，不应让界面显得松散。
- 视频卡片可使用 poster、播放图标、时长和规格标签；poster 不可用时使用克制 fallback，不要在网格中放真实 video 元素批量加载。时长、状态、数量等短文本标签需要背景承载时，应使用项目一致的玻璃态圆角矩形，禁止使用突兀的椭圆胶囊徽标；只有纯图标圆形按钮、头像、单点状态等明确需要圆形的元素才使用 `--radius-full`。
- 视频重建模型在模型列表中应复用现有 item hover 操作层：下载、删除、原视频预览等图标按钮只在 hover/focus 或触控可达状态出现，不要为视频模型单独改成常驻大按钮。缩略图优先使用源视频封面帧，缺失时用现有克制 fallback。
- 视频重建弹窗的选项说明可以使用小标题/副文案解释档位差异，但布局密度应接近 Settings；移动端分段控件可降为单列卡片，不能横向溢出或让底部按钮被内容遮挡。

### 模型资产库视觉细节

- 模型资产库顶部工具栏、分段筛选、排序、格式、标签、密度和操作按钮应复用本地媒体图库的玻璃态层级和圆角语言；同一工具栏内不要混用大圆角胶囊、小方角按钮和高饱和蓝色背景。
- 工具栏必须是真正的悬浮/粘性玻璃表面：背后的模型卡片滚动时应能从玻璃下方透出，不能用实心黑色背景或额外边框切断内容。
- 资产库普通浏览不显示分页器或“每页数量”选择；滚动接近底部时按后端游标加载下一批，密度控件只控制展示列数。
- 模型卡片桌面端点击按钮之外区域直接打开 viewer；hover/focus 操作按钮保持独立圆形图标按钮，详情按钮语义必须和打开预览、下载、删除区分清晰。
- 触控端不能依赖 hover。移动端点击卡片可打开详情卡片，再通过详情卡片执行打开、下载、导出、删除；详情卡片的缩略图本身可打开原图/视频，不额外叠加“查看原图”图标。
- 详情面板应尽量复用桌面端信息结构，只通过响应式尺寸和排列适配移动端；不要为移动端单独创造一套割裂的按钮、圆角和色彩系统。四个主要操作在窄屏下应并排或紧凑排列，避免挤压 Details/Metadata 区域。
- 近期模型侧栏列表应保持标题、查看全部入口和底部“储存占用”固定，只有列表内容滚动；滚动条使用克制灰色，不使用突兀蓝色。
- 近期模型条目的查看、下载、删除按钮应复用桌面 hover item 的独立圆形按钮样式；触控或深色模式下可以常驻，但不要用三个按钮共享一个胶囊背景。

### 视频预览视觉细节

- 视频控制栏应是小巧悬浮玻璃层，不默认铺满预览窗口宽度；播放/seek 控制居中，音量靠左，下载/全屏靠右。
- 进度条和音量条使用简洁粗线条，不使用过大的胶囊底座；hover/focus 状态要清晰但不抢画面。
- 控制栏播放中应支持自动隐藏；文件名、下载、关闭、全屏和控制栏不能互相遮挡，也不能遮住横屏视频的核心画面。
- 全屏和退出全屏应使用不同图标状态；图标按钮必须有 tooltip/aria-label。
- 播放失败态使用与播放器一致的玻璃态卡片、克制图标容器和玻璃按钮；不要使用突兀的大蓝胶囊按钮或临时错误条。
- 移动端折叠控制栏点击非按钮区域可展开，但不应强制滚动页面；展开/折叠变化不能导致画面和按钮跳动。

### 局域网门禁视觉细节

- 门禁页、启动提醒和设置页门禁区域必须延续现有 Apple 玻璃态：柔和半透明背景、清晰层级、克制边框和足够圆角；不要使用浏览器原生弹窗或临时拼装的提示块。
- 启动提醒应是轻量 modal：标题、风险说明、2-3 个主要动作即可，按钮间距和卡片内边距要匹配现有 Settings / Modal 视觉密度。
- 设置页门禁关闭时只显示总开关、风险提示和保存动作；开启后再展开访问码、会话天数、远程生成和撤销会话，并用明显留白分隔开关卡片与详细表单。
- 安全警告可以使用暖色强调，但面积要克制；不要让警告色压过主操作，也不要用大块高饱和纯色破坏整体质感。

---

## 深色模式策略

统一使用 `@media (prefers-color-scheme: dark)` 覆盖：

- 背景透明度与边框强度
- 阴影强度
- 文本可读性

建议采用“最小覆盖原则”：只覆盖在深色下确实需要变化的属性，避免整段样式重复。

---

## 层级（z-index）约定

当前项目存在多层浮层叠加，建议遵循以下区间：

- 0~10：背景与主内容层
- 15~35：侧栏、帮助面板、查看器特效轨道、导航辅助层
- 50~100：加载遮罩、控制面板、快捷控制、设置弹窗
- 1000：全局最高优先级查看器（如图片灯箱）

新增浮层前先确认是否会与现有 `Help`、`Settings`、`ImageViewer` 发生遮挡冲突。

---

## 反模式（新增代码禁止）

- 用 emoji 充当功能图标（应使用 SVG 图标组件）
- 用内联样式承载长期视觉规则（动态计算样式除外）
- 在组件中新增全局选择器污染（除 `:global(...)` 明确必要场景）
- 新增硬编码魔法值且无注释/无 token 归属
- 新增 Tailwind、Sass、CSS-in-JS

---

## 交付前检查清单

- [ ] 组件样式是否全部使用 CSS Modules
- [ ] 新 token 是否仅添加在 `variables.css`
- [ ] 深色模式下文本与边框可读性是否达标
- [ ] 关键交互是否有 hover + focus-visible 状态
- [ ] 触控设备下可操作元素是否可达
- [ ] 是否避免了 emoji 图标与重复关键帧
- [ ] 375 / 768 / 1024 / 1440 宽度下无布局溢出
