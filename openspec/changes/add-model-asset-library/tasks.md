## 1. 后端资产基础

- [x] 1.1 在 `backend/paths.py` 中增加模型资产导入目录、模型资产缩略图目录和资产索引路径，并将受控模型资产目录纳入静态文件 allowlist。
- [x] 1.2 新增或扩展模型资产服务，定义支持格式、资产 ID、索引读写、原子写入、文件存在校验和旧 `outputs/*.ply/.spz` 兼容映射。
- [x] 1.3 为现有生成模型补齐资产摘要字段，包括名称、来源、格式列表、大小、创建/修改时间、缩略图状态、用户编辑字段和默认打开 URL。
- [x] 1.4 实现资产列表查询的游标增量加载、来源筛选、格式筛选、标签筛选、排序和总数统计，确保普通列表读取不依赖全量缩略图生成。
- [x] 1.5 实现资产详情查询，返回详情面板所需的文件、格式、来源、点数/包围盒占位、源媒体和元数据字段。

## 2. 后端导入与封面 API

- [x] 2.1 新增 `/api/model-assets` 列表与详情端点，并保持旧 `/api/gallery` 行为可用或通过兼容层返回旧结构。
- [x] 2.2 新增模型导入端点，支持单文件和批量上传 PLY、SPZ、SPLAT、RAD，并返回成功资产与逐项失败原因。
- [x] 2.3 为导入端点添加文件名净化、扩展名校验、单文件大小限制、批次大小限制和受控目录写入校验。
- [x] 2.4 新增资产资料编辑端点，支持修改显示名称、标签和备注，且不重命名、不移动源模型文件。
- [x] 2.5 新增模型封面上传或刷新端点，校验资产 ID、图片类型、图片大小和目标路径后写入受控缩略图缓存，并支持手动封面优先展示。
- [x] 2.6 新增或适配模型资产下载、删除、默认打开 URL 和缩略图 URL 逻辑，确保路径不会暴露任意本机绝对路径。
- [x] 2.7 更新 `backend/services/static_files.py` 相关路径解析，确认模型导入文件和缩略图只能从显式 allowlist 根目录提供。

## 3. 权限与安全

- [x] 3.1 更新 `backend/security/access_control.py`，将模型资产读取端点归入私有读取资源。
- [x] 3.2 将模型资产导入、资产资料编辑与封面写入端点归入现有“允许远程生成”写入权限：本机 owner 可用，远程需认证且开启允许远程生成。
- [x] 3.3 保持删除、配置、批量转换等 owner-only 管理动作不因模型资产库开放而降级。
- [x] 3.4 为路径穿越、符号链接逃逸、敏感文件请求、未授权远程导入、未授权封面写入添加后端测试。
- [x] 3.5 为旧 `/api/gallery`、新 `/api/model-assets`、导入、缩略图和下载端点补充 route map 或 API 契约测试。

## 4. 前端数据层

- [x] 4.1 新增 `frontend/src/types/modelAsset.ts`，定义模型资产、筛选、排序、游标浏览、导入结果、封面状态、用户编辑字段和详情字段类型。
- [x] 4.2 新增 `frontend/src/api/modelAssets.ts`，通过现有 fetch client 实现列表、详情、导入、资料编辑、封面上传、下载、删除和刷新调用。
- [x] 4.3 更新 `frontend/src/api/index.ts` 与 `frontend/src/types/index.ts` 导出新模型资产 API 和类型。
- [x] 4.4 扩展 Zustand store，保存模型资产列表、筛选、排序、密度、加载游标、选中资产、选择模式、导入状态、编辑状态和错误状态。
- [x] 4.5 保持现有 viewer 加载状态与模型资产选择联动，打开资产时复用当前 PLY/SPZ/SPLAT/RAD 加载流程。

## 5. 前端资产库 UI

- [x] 5.1 新建 `ModelAssetLibraryView` 组件三件套，承载左侧辅助栏、中间网格工作台和右侧详情面板。
- [x] 5.2 新建模型资产工具栏组件，包含来源/格式/标签筛选、排序菜单、密度控制、刷新、导入、选择模式和打开按钮。
- [x] 5.3 新建模型资产网格与卡片组件，展示封面、格式徽标、名称、来源、时间/大小摘要、选中状态和快捷操作。
- [x] 5.4 新建模型资产详情面板，展示大封面、标题、主要操作、Details/Metadata 标签、用户编辑入口和缺失字段占位。
- [x] 5.5 将模型入口主区域切换为资产库工作台，侧栏改为辅助导航、任务队列、近期模型和存储摘要，不破坏图库入口。
- [x] 5.6 实现响应式布局：桌面尽量还原示例图三栏结构，窄屏无横向溢出，核心操作不依赖 hover。
- [x] 5.7 使用 CSS Modules 和 CSS Variables 实现项目现有 Apple 毛玻璃风格、系统强调色 token、稳定卡片尺寸、焦点状态和深浅色模式适配，避免 Tailwind、CSS-in-JS、Sass。
- [x] 5.8 按示例图补齐视觉还原细节：三栏比例、标题计数、筛选分段控件、工具栏按钮顺序、卡片悬浮操作、选中勾选态、右侧操作区、Details/Metadata 标签和滚动增量加载边界。
- [x] 5.9 准备一组用于视觉验收的样本资产数据，覆盖生成、导入、视频关联、PLY、SPZ、SPLAT、RAD、缺失封面、长名称和选中状态。

## 6. 导入体验与封面生成

- [x] 6.1 实现本机文件选择、拖拽导入和批量导入 UI，显示上传进度、成功数量和逐项失败反馈。
- [x] 6.2 将现有拖拽模型临时预览行为改为可选择导入资产库或直接临时打开，避免用户误以为已保存。
- [x] 6.3 实现导入模型封面生成 hook 或服务，使用浏览器端 Three/Spark 离屏渲染生成小尺寸封面并上传缓存。
- [x] 6.4 为封面生成增加并发限制、可见项优先、失败缓存和格式感知占位，避免大模型导致主界面卡顿。
- [x] 6.5 生成模型继续优先使用源图片或视频帧缩略图，导入模型优先使用缓存封面，所有卡片都保持稳定占位状态。
- [x] 6.6 实现手动封面上传、替换和恢复系统封面的前端交互，并确保手动封面优先于系统生成封面。

## 7. 资料编辑体验

- [x] 7.1 实现资产名称、标签和备注编辑 UI，保存后同步刷新卡片、详情面板和筛选结果。
- [x] 7.2 实现编辑失败、权限不足、字段校验失败和保存中状态的本地化反馈。
- [x] 7.3 确认编辑资产资料不会改变资产 ID、模型文件名、下载 URL 或 viewer 打开路径。

## 8. 国际化与可访问性

- [x] 8.1 在 `frontend/src/i18n/zh.json` 中添加模型资产库所有新增中文文案。
- [x] 8.2 在 `frontend/src/i18n/en.json` 中添加对应英文文案，并检查 key 命名与现有约定一致。
- [x] 8.3 为工具栏、卡片操作、导入、编辑、选择模式、详情标签、错误和权限提示添加语义标签与可见焦点。
- [x] 8.4 检查触摸设备路径，确保选择、打开、下载、删除、编辑和导入都有可见控件入口。

## 9. 回归与验收

- [x] 9.1 运行 `python -m pytest tests`，修复模型资产 API、安全、路径和兼容层相关失败。
- [x] 9.2 运行 `npm run lint` 于 `frontend/`，修复 TypeScript/React lint 问题。
- [x] 9.3 运行 `npm run build` 于 `frontend/`，确认类型检查和 Vite 构建通过。
- [x] 9.4 使用至少 200 个模型资产样本验证列表首屏、滚动、筛选、排序、选择和详情面板响应性。
- [x] 9.5 使用本机和局域网授权客户端分别验证模型导入、资料编辑、未授权拒绝、封面写入、刷新后持久存在和旧生成模型兼容。
- [x] 9.6 使用 Playwright 或人工截图验证桌面宽屏尽可能还原示例图，移动/窄屏无文本溢出、控件重叠或横向滚动。
- [x] 9.7 回归旧模型生成、旧 `/api/gallery`、下载、删除、viewer 打开、照片图库入口和任务队列显示。
- [x] 9.8 保存或记录桌面宽屏、平板宽度和移动宽度的验收截图，并在浅色/深色系统主题下对照视觉验收基线检查三栏结构、工具栏顺序、卡片状态、详情面板、滚动加载区域、项目 Apple 毛玻璃层级和长文本表现。

## 10. 验收后回归修复

- [x] 10.1 修复宽屏首批资产不足以产生滚动条时无法继续游标加载的问题，并确保刷新、密度变化和窗口尺寸变化后会自动补批到可滚动边界。
- [x] 10.2 修复工作目录切换后的自动重启链路：安全替换后端进程，并由前端确认新服务实例已使用目标 workspace 后再刷新页面。
- [x] 10.3 回归验证模型资产游标 API、重启 API 契约、后端 pytest、前端 lint 和生产构建。

## 11. 模型资产库返回上下文与内存治理

- [x] 11.1 复用当前筛选组合已加载的模型资产摘要与游标，保存并恢复桌面/窄屏滚动位置，避免从 viewer 或侧栏“查看全部”返回时重新请求首批并回到顶部。
- [x] 11.2 保持组件卸载语义并治理后台资源：卸载时停止继续泵入封面生成队列，限制全局缩略图状态缓存规模，确保多次进入/退出不会累积 DOM、监听器或无界缓存。
- [x] 11.3 回归验证返回上下文、增量加载、筛选重置、前端 lint/构建、完整 pytest 与 OpenSpec 严格校验。

## 12. 外部模型拖拽入库体验

- [x] 12.1 区分主模型页与资产库拖拽语义：主模型页单文件保持临时预览并提供“加入资产库”，资产库页面单个或多个模型直接批量入库。
- [x] 12.2 新增符合现有玻璃态设计语言的批量导入进度弹窗，展示上传进度、成功/失败汇总和逐项失败原因，并补齐中英文与可访问语义。
- [x] 12.3 统一工具栏文件选择、资产库拖拽、主页面多文件拖拽和临时预览入库的数据刷新路径，完成后保留资产库浏览上下文并释放 File 引用。
- [x] 12.4 回归验证拖拽分流、权限失败、部分成功、大批次列表状态、前端 lint/构建与 OpenSpec 严格校验。
